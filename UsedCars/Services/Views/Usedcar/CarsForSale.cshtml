@model DTOs.SearchResultDTO
@using UsedCarUI.Models
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/CSS/frontpage.css">
    <title>CarsForSale</title>

</head>
<body>
    <h1>Used Car For Sale</h1>
    <div id="filter">
        <p id="filterByLocation">
            Filter By Location <span>
                @Html.DropDownList("cities", new SelectList(Enum.GetValues(typeof(citylist))),
                                                                              "Select",
                                                                              new { @class = "cityFilter" })
            </span>
        </p>
        <p id="filterByBudget">
            Filter By Budget <span><input type="number" id="minPrice"></span>
            <span><input type="number" id="maxPrice"></span>
            <button onclick="filterByPrice()" id="filterByBudgetButton">GO</button>
        </p>

    </div>
    <div id="showCars">
        @{Html.RenderPartial("~/Views/Shared/UsedCar.cshtml", Model);}
    </div>

    @*<div class="pageButton">
            @if (Model.PreviousPageId != 0)
            {
                <button id="previousButton" value="@Model.PreviousPageId">Previous</button>
            }
            @if (Model.totalStock > (Model.NextPageId + 1))
            {
                <button id="nextButton" value="@Model.NextPageId">Next</button>
            }
        </div>*@


    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/UsedCarScript.js"></script>
</body>
</html>